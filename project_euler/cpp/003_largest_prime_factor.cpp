#include <cstdlib>
#include <iostream>

unsigned long find_largest_prime_factor(unsigned long x){
    return 0;
}

int main(int argc, char **argv){
  if (argc >= 1){
    std::cout << static_cast<unsigned long>(atoi(argv[1])) << std::endl;
  }
  return 0;
}
